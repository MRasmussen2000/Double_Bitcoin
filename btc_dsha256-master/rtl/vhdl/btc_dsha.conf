# btc_dsha.conf
# Design configuration for btc_dsha

# Set a maximum area constraint (adjust as required)
set_max_area 55000

# Additional configuration parameters (e.g., floorplan or timing constraints) can be added here.